type Recipe {
  id: ID!
  name: String!
}

input NewRecipe {
  name: String!
}

extend type Query {
  getRecipe(id: ID!): Recipe!
  getAllRecipes: [Recipe]!
}

type Mutation {
  createRecipe(name: String): Recipe!
}
