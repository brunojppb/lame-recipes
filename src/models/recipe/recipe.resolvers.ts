import { Resolver } from 'dns';
import { GraphQLContext, ResolverInterface } from '../../interfaces';

export const getRecipe: ResolverInterface = (args: { id: string }, ctx: any): any => {
  return null;
};
